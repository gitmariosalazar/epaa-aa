const http = require('http');

const data = JSON.stringify({
  username_or_email: "TERAN TERAN", 
  password: "password123" // Just a guess, or I don't know the password.
});

// Actually I don't know a valid password. Let me just search the logs to see what error the backend threw, if any.
