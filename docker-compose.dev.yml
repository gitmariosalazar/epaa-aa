services:
  client-gateway-epaa-dev:
    image: client-gateway-epaa-dev:development
    build:
      context: ./client-gateway
      dockerfile: dockerfile.dev
    restart: unless-stopped
    container_name: client-gateway-epaa-dev
    ports:
      - '${GATEWAY_SERVER_PORT_DEVELOPMENT:-4005}:${GATEWAY_SERVER_PORT_DEVELOPMENT:-4005}'
    volumes:
      - ./client-gateway:/usr/src/app
      - client-gateway-node-modules:/usr/src/app/node_modules
      - /home/sigepaa/sigepaa/images/connections:/home/sigepaa/sigepaa/images/connections
      - /home/sigepaa/sigepaa/images/qrcodes:/home/sigepaa/sigepaa/images/qrcodes
      - /home/sigepaa/sigepaa/images/readings:/home/sigepaa/sigepaa/images/readings
      - //home/sigepaa/sigepaa/images/work_orders:/home/sigepaa/sigepaa/images/work_orders
    command: ['npm', 'run', 'start:dev']
    env_file:
      - ./client-gateway/.env.development
    environment:
      - CHOKIDAR_USEPOLLING=true
      - NODE_OPTIONS=--max-old-space-size=1024
    networks:
      - epaa-network-dev
    healthcheck:
      test:
        [
          'CMD',
          'curl',
          '-f',
          'http://localhost:${GATEWAY_SERVER_PORT_DEVELOPMENT:-4005}/health'
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1536M
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  readings-service-dev:
    image: readings-service-dev:development
    build:
      context: ./readings
      dockerfile: dockerfile.dev
    restart: unless-stopped
    container_name: readings-service-dev
    ports:
      - '${READINGS_SERVER_PORT_DEVELOPMENT:-4007}:${READINGS_SERVER_PORT_DEVELOPMENT:-4007}'
    volumes:
      - ./readings:/usr/src/app
      - readings-service-node-modules:/usr/src/app/node_modules
    command: ['npm', 'run', 'start:dev']
    env_file:
      - ./readings/.env.development
    environment:
      - CHOKIDAR_USEPOLLING=true
      - NODE_OPTIONS=--max-old-space-size=1024
    networks:
      - epaa-network-dev
    healthcheck:
      test: ['CMD', 'true']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1536M
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  epaa-database-legacy-service-dev:
    image: epaa-database-legacy-service-dev:development
    build:
      context: ./epaa-database-legacy
      dockerfile: dockerfile.dev
    restart: unless-stopped
    container_name: epaa-database-legacy-service-dev
    ports:
      - '${EPAA_DATABASE_LEGACY_SERVER_PORT_DEVELOPMENT:-4009}:${EPAA_DATABASE_LEGACY_SERVER_PORT_DEVELOPMENT:-4009}'
    volumes:
      - ./epaa-database-legacy:/usr/src/app
      - epaa-database-legacy-node-modules:/usr/src/app/node_modules
    command: ['npm', 'run', 'start:dev']
    env_file:
      - ./epaa-database-legacy/.env.development
    environment:
      - CHOKIDAR_USEPOLLING=true
      - NODE_OPTIONS=--max-old-space-size=1024
    networks:
      - epaa-network-dev
    healthcheck:
      test: ['CMD', 'true']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1536M
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  connection-service-dev:
    image: connection-service-dev:development
    build:
      context: ./connection
      dockerfile: dockerfile.dev
    restart: unless-stopped
    container_name: connection-service-dev
    ports:
      - '${CONNECTION_SERVER_PORT_DEVELOPMENT:-4013}:${CONNECTION_SERVER_PORT_DEVELOPMENT:-4013}'
    volumes:
      - ./connection:/usr/src/app
      - connection-service-node-modules:/usr/src/app/node_modules
    command: ['npm', 'run', 'start:dev']
    env_file:
      - ./connection/.env.development
    environment:
      - CHOKIDAR_USEPOLLING=true
      - NODE_OPTIONS=--max-old-space-size=1024
    networks:
      - epaa-network-dev
    healthcheck:
      test: ['CMD', 'true']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1536M
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  security-service-dev:
    image: security-service-dev:development
    build:
      context: ./authentication
      dockerfile: dockerfile.dev
    restart: unless-stopped
    container_name: security-service-dev
    ports:
      - '${SECURITY_SERVER_PORT_DEVELOPMENT:-4004}:${SECURITY_SERVER_PORT_DEVELOPMENT:-4004}'
    volumes:
      - ./authentication:/usr/src/app
      - security-service-node-modules:/usr/src/app/node_modules
    command: ['npm', 'run', 'start:dev']
    env_file:
      - ./authentication/.env.development
    environment:
      - CHOKIDAR_USEPOLLING=true
      - NODE_OPTIONS=--max-old-space-size=1024
    networks:
      - epaa-network-dev
    healthcheck:
      test: ['CMD', 'true']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1536M
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  clients-service-dev:
    image: clients-service-dev:development
    build:
      context: ./clients
      dockerfile: dockerfile.dev
    restart: unless-stopped
    container_name: clients-service-dev
    ports:
      - '${CLIENTS_SERVER_PORT_DEVELOPMENT:-4006}:${CLIENTS_SERVER_PORT_DEVELOPMENT:-4006}'
    volumes:
      - ./clients:/usr/src/app
      - clients-service-node-modules:/usr/src/app/node_modules
    command: ['npm', 'run', 'start:dev']
    env_file:
      - ./clients/.env.development
    environment:
      - CHOKIDAR_USEPOLLING=true
      - NODE_OPTIONS=--max-old-space-size=1024
    networks:
      - epaa-network-dev
    healthcheck:
      test: ['CMD', 'true']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1536M
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  companies-service-dev:
    image: companies-service-dev:development
    build:
      context: ./companies
      dockerfile: dockerfile.dev
    restart: unless-stopped
    container_name: companies-service-dev
    ports:
      - '${COMPANIES_SERVER_PORT_DEVELOPMENT:-4008}:${COMPANIES_SERVER_PORT_DEVELOPMENT:-4008}'
    volumes:
      - ./companies:/usr/src/app
      - companies-service-node-modules:/usr/src/app/node_modules
    command: ['npm', 'run', 'start:dev']
    env_file:
      - ./companies/.env.development
    environment:
      - CHOKIDAR_USEPOLLING=true
      - NODE_OPTIONS=--max-old-space-size=1024
    networks:
      - epaa-network-dev
    healthcheck:
      test: ['CMD', 'true']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1536M
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  location-service-dev:
    image: location-service-dev:development
    build:
      context: ./location
      dockerfile: dockerfile.dev
    restart: unless-stopped
    container_name: location-service-dev
    ports:
      - '${LOCATION_SERVER_PORT_DEVELOPMENT:-4010}:${LOCATION_SERVER_PORT_DEVELOPMENT:-4010}'
    volumes:
      - ./location:/usr/src/app
      - location-service-node-modules:/usr/src/app/node_modules
    command: ['npm', 'run', 'start:dev']
    env_file:
      - ./location/.env.development
    environment:
      - CHOKIDAR_USEPOLLING=true
      - NODE_OPTIONS=--max-old-space-size=1024
    networks:
      - epaa-network-dev
    healthcheck:
      test: ['CMD', 'true']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1536M
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  qrcode-service-dev:
    image: qrcode-service-dev:development
    build:
      context: ./qrcode
      dockerfile: dockerfile.dev
    restart: unless-stopped
    container_name: qrcode-service-dev
    ports:
      - '${QR_CODE_SERVER_PORT_DEVELOPMENT:-4012}:${QR_CODE_SERVER_PORT_DEVELOPMENT:-4012}'
    volumes:
      - ./qrcode:/usr/src/app
      - qrcode-service-node-modules:/usr/src/app/node_modules
      - /home/mariosalazar/Desktop/Epaa/images/qrcodes:/home/mariosalazar/Desktop/Epaa/images/qrcodes
    command: ['npm', 'run', 'start:dev']
    env_file:
      - ./qrcode/.env.development
    environment:
      - CHOKIDAR_USEPOLLING=true
    networks:
      - epaa-network-dev
    healthcheck:
      test: ['CMD', 'true']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1536M
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

volumes:
  client-gateway-node-modules:
  qrcode-service-node-modules:
  readings-service-node-modules:
  epaa-database-legacy-node-modules:
  #property-service-node-modules:
  clients-service-node-modules:
  companies-service-node-modules:
  connection-service-node-modules:
  #work-orders-service-node-modules:
  #sigame-legacy-node-modules:
  workers-service-node-modules:
  security-service-node-modules:
  location-service-node-modules:

networks:
  epaa-network-dev:
    external: true
